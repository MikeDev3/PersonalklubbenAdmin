@model IEnumerable<PersonalklubbenHVAdmin.Models.Nyheter>
@{
    ViewBag.Title = "Index";
}


@if (Model.Count() > 0)
{
    <div class="table-users table-wrapper-scroll-y my-custom-scrollbar">
        <div class="customHeader">
            Senaste nytt

            @*<button class="createNewPostButton">@Html.ActionLink("Skapa nyhet", "CreateNews", "Nyheter", null, new { @class = "logoutBtnColor btn " }) <span class="glyphicon glyphicon-cog"></span></button>*@
            @*<input style="margin-left: 50px" type="button" class="createNewPostButton logoutBtnColor btn " value="Skapa nyhet!" onclick="location.href='@Url.Action("CreateNews", "Nyheter")'" />
            <div>
                @Html.ActionLink("Skapa nyhet", "CreateNews", "Nyheter", null, new { @class = "createNewPostButton logoutBtnColor btn " })
                <span class="glyphicon glyphicon-cog"> </span>
                </div>*@
                
            <a href="@Url.Action("CreateNews", "Nyheter")" class="createNewPostButton logoutBtnColor btn"><span class="glyphicon glyphicon-cog" ></span> Skapa nyhet</a>

            </div>
           
            
   

        <table cellspacing="0" style="overflow-y: auto">
            <tr>
                <th></th>
                <th>Nyhet</th>
                <th>Underrubrik</th>
                <th></th>
                <th width="630">Innehåll</th>
                <th>Radera</th>
                <th>Ändra</th>

            </tr>

            @foreach (var item in Model)
            {
            <tr>
                <td><img class="profileImg" src="~/Images/MessageIcon.png" alt="MeddelandeIkon" /></td>
                <td style="font-family:'Segoe UI', Tahoma, Geneva, Verdana, sans-serif; font-weight:bold; font-size: 15px;">
                    @Html.DisplayFor(m => item.Rubrik)<br /> <br />
                    @item.PubliceringsDatum.ToString("yyyy/MM/dd")
                </td>
                <td>@Html.DisplayFor(m => item.Underrubrik)</td>
                <td></td>
                <td>@Html.DisplayFor(m => item.Beskrivning)</td>
                <td>
                    @*<button class="blockBtn"><span class="glyphicon glyphicon-trash" onclick="return myFunction()"></span>&nbsp;@Html.ActionLink("Radera", "DeleteNewsPost", new { id = item.ID }, new { onclick = "return confirm('Är du säker på att du vill radera denna nyhet?');" })</button>
        <button class="editBtn"><span class="glyphicon glyphicon-cog"></span>&nbsp;Ändra</button>*@
                    <a href="@Url.Action("DeleteNewsPost", "Nyheter", new { id = item.ID })" onclick="return confirm('Är du säker på att du vill radera denna nyhet?');" class="blockBtn logoutBtnColor btn"><span class="glyphicon glyphicon-trash"></span> Radera</a>

                </td>
                <td>
                    <a href="@Url.Action("EditNews", "Nyheter", new { id = item.ID })" class="editBtn logoutBtnColor btn"><span class="glyphicon glyphicon-cog"></span> Ändra</a>

                </td>
            </tr>

           
            }
        </table>
    </div>
}
else
{
    <h2>Inga nyheter att visa</h2>
}

   